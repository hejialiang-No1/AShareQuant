# 更新记录

AShareQuant 的所有重要变更都会记录在此。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.0] - 2026-09-10

### 新增

- **筹码分布（CYQ）**（`src/shared/chip.js`）
  - 借鉴开源 A 股工具（myhhub/stock、a-stock-data-quant）的筹码模型：
    价格轴切 90 个等宽价格桶 → 逐日按「三角核」把成交量摊到当日 `[low, high]`（越靠近均价权重越高）
    → 已有筹码按换手率衰减 → 归一化
  - 输出获利比例、平均成本、90% 成本区间、集中度与筹码峰
  - 无换手率数据时用「成交量 / 20 日均量」近似活跃度，保证纯 K 线也能计算
- **支撑 / 压力位识别**（`src/shared/levels.js`）
  - 三种来源合成：摆动高低点聚类（触碰次数 = 强度）、经典枢轴点 P/R1/R2/S1/S2、
    斐波那契回撤 23.6% / 38.2% / 50% / 61.8% / 78.6%
- **K 线形态识别**（`src/shared/patterns.js`）
  - 覆盖 A 股最常用的单根 / 两根 / 三根组合形态，共 19 种：
    锤子线、上吊线、倒锤头、射击之星、十字星、大阳线、大阴线、长下影、长上影、
    看涨吞没、看跌吞没、乌云盖顶、曙光初现、孕线、早晨之星、黄昏之星、红三兵、三只乌鸦
  - 每种形态输出方向（bull / bear / neutral）与 1–3 级强度
- **买卖点信号引擎**：信号生成 + K 线图上直接标注 + 分析面板汇总
- **持仓管理视图**：成本、市值、浮动盈亏一屏查看
- **市场情绪指标**：量化当前市场冷热程度

### 变更

- **界面按 Apple 审美全面重构**，对齐 macOS Liquid Glass 设计语言
  - 毛玻璃质感：`backdrop-filter: blur(40px) saturate(1.8)` + 镜面高光 + 亮边缘描边
  - 统一圆角语言与深色 / 浅色主题变量

### 修复

- **K 线主图完全空白（真 bug）**
  - 现象：`--smoke` 自检报画布 0 像素，K 线主图区没有任何绘制内容
  - 根因：数据源输出短字段名 `{t,o,h,l,c,v}`，而绘图代码读长字段名 `{date,open,high,low,close,volume}`，
    导致 `b.low` / `b.high` 为 `undefined` → `lo = Infinity` → 坐标映射全部 NaN
  - 修复：六个 shared 模块与 chart/app 统一采用双字段名兼容（`b.low != null ? b.low : b.l`）
- **KPI 面板 8 个格子只显示 `--`**
  - 根因：`factors.metrics` 缺少 `ma120 / macdHist / kdjK / volatility / hi52 / lo52` 字段，
    且涨幅字段命名不一致（`ret5/ret20/ret60` 与消费方读取的 `r5/r20/r60` 对不上）
  - 修复：补齐字段并统一命名
- **均线涨跌颜色恒为绿色**
  - 根因：读取了不存在的 `metrics.price`，应读分析结果的顶层 `price`
  - 修复：改读 `analyze().price`

### 验证

- 算法层自检 `npm run smoke`：**52 项全部通过**（含 T+1 约束三项断言）
- 渲染层自检 `--smoke`：`errors: []`、`canvasPainted: true`、K 线满幅绘制（首行 16 → 末行 908）、
  筹码分布绘制像素 22065、信号 20 条、支撑压力 8 条、形态 24 条

## [1.0.0] - 2026-09-10

### 新增

- 首个正式版本：行情、分析、选股、回测、预警、板块六大模块
- 腾讯（主）→ 新浪（备）双源自动降级行情链路，任一源限流或故障自动切换；内置 T+1 与涨跌停约束
- 策略回测：MACD、RSI、布林带、KDJ、涨停等
- 导出 CSV、macOS 应用打包（Apple Silicon 原生）
